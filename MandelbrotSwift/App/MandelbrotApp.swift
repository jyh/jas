import SwiftUI
import MandelbrotLib

@main
struct MandelbrotExplorerApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .defaultSize(width: 1200, height: 900)
    }
}
