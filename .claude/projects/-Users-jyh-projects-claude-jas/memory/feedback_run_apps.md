---
name: Only run apps when asked
description: Don't run all 3 apps automatically — only run when user explicitly asks
type: feedback
---

Only run apps when the user explicitly asks. Don't automatically run all three (Python, OCaml, Swift) apps after implementing a feature.

**Why:** User finds it unnecessary/annoying when apps are launched without being asked.
**How to apply:** After implementing a feature, report completion and wait for the user to decide what to run or whether to commit.
